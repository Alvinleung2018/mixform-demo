import React from 'react';
import {Layout} from 'antd';
const {Footer} = Layout;

export default class FooterExt extends React.Component {
    render() {
        return (
            <Footer>footer</Footer>
        )
    }
}